import React from 'react';
import { Layout } from 'antd';
import './index.css';

const Footer = () => {
  return (
    <Layout.Footer className="footer">
    </Layout.Footer>
  );
};

export default Footer;
